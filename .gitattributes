# Set default behaviour to automatically normalize line endings.
* text=auto
# Force BASH scripts to always use Unix-style line endings.
*.sh text eol=lf
assets/random_forest.joblib filter=lfs diff=lfs merge=lfs -text
